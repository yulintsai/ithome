kind: pipeline
type: docker
name: backend

steps:
- name: test
  image: docker:dind
  volumes:
  - name: dockersock
    path: /var/run/docker.sock
  commands:
  - docker build --no-cache --pull --force-rm -t rain/ithome:latest -f go.dockerfile .

trigger:
  event:
  - tag

volumes:
- name: dockersock
  host:
    path: /var/run/docker.sock